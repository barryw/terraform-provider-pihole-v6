---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pihole_setting Resource - pihole"
subcategory: ""
description: |-
  Manages a PiHole configuration setting. Values are JSON-encoded — use jsonencode() in your Terraform config.
---

# pihole_setting (Resource)

Manages a PiHole configuration setting. Values are JSON-encoded — use jsonencode() in your Terraform config.

## Example Usage

```terraform
resource "pihole_setting" "app_sudo" {
  key   = "webserver.api.app_sudo"
  value = jsonencode(true)
}

resource "pihole_setting" "block_ttl" {
  key   = "dns.blockTTL"
  value = jsonencode(2)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `key` (String) Dot-notation config path (e.g. webserver.api.app_sudo).
- `value` (String) JSON-encoded value. Use jsonencode(true), jsonencode(86400), jsonencode("NULL"), etc.

### Read-Only

- `id` (String) Same as key.
